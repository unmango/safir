<c:SafirWindow x:Class="Safir.Views.MainView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Safir.Views"
        xmlns:c="clr-namespace:Safir.Controls"
        mc:Ignorable="d"
        Title="{Binding AppName}">
    <Grid>
        <StackPanel Background="{DynamicResource WindowBackgroundColorBrush}">
            <Menu>
                <MenuItem Header="Item1"/>
                <MenuItem Header="Item2"/>
            </Menu>
        </StackPanel>
        <Button Height="400" Width="400">A button</Button>
    </Grid>
</c:SafirWindow>
