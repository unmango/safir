<c:SafirWindow x:Class="Safir.Views.MainView"
               xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
               xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
               xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
               xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
               xmlns:c="clr-namespace:Safir.ControlLibrary.Controls;assembly=Safir.ControlLibrary"
               xmlns:vm="clr-namespace:Safir.ViewModels"
               mc:Ignorable="d"
               d:DataContext="{d:DesignInstance vm:MainViewModel}"
               Title="{Binding AppName}"
               Icon="/Safir;component/Resources/Icons/brand.ico">
    <Grid>
        <DockPanel>
            <ContentControl x:Name="MainMenu" Height="20" DockPanel.Dock="Top" VerticalAlignment="Top" />
            <Button Height="400" Width="400">A button</Button>
        </DockPanel>
    </Grid>
</c:SafirWindow>
