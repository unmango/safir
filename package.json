{
  "name": "safir",
  "private": true,
  "license": "GPL-3.0-or-later",
  "repository": {
    "type": "git",
    "url": "https://github.com/unmango/safir.git",
    "directory": "src/common/node"
  },
  "scripts": {
    "build": "yarn workspaces foreach -vpt run build",
    "publish": "yarn workspaces foreach -vpt run publish",
    "test": "yarn workspaces foreach run test"
  },
  "workspaces": [
    "src/common/node/packages/*",
    "src/ui"
  ],
  "packageManager": "yarn@3.2.0"
}
