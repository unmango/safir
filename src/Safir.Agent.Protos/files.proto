syntax = "proto3";
import "google/protobuf/empty.proto";

option csharp_namespace = "Safir.Agent.Protos";

service FileSystem {
  rpc List(google.protobuf.Empty) returns (stream FileSystemEntry);
}

message FileSystemEntry {
  string path = 1;
}
