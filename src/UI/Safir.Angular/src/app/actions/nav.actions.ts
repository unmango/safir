import { createAction, props } from '@ngrx/store';

export const collapse = createAction('[Nav] Collapse');
export const expand = createAction('[Nav] Expand');
export const toggle = createAction('[Nav] Toggle');
